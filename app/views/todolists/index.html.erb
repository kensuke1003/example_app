<div class="list-wrap">
<% @lists.each do |list| %>
  <divn class="list-box">
    <%= attachment_image_tag list, :image,:fill,200,200,format:'jpeg' %>
    <div class="list-comment">
      <span class="list-body"><%= list.body %></sapn>
      <span class="lsit-title"><%= link_to list.title, todolist_path(list.id) %></span>
    </div>
<% end %>
</div>

